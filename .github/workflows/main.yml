name: Build Project
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Environment
        run: |
          echo "Starting the build process..."
          ls -R

      - name: Build with Make (نموونە)
        run: |
          # ئەگەر پڕۆژەکەت Makefile ی تێدایە ئەمە لێ بدە
          # ئەگەر نا، لێرەدا فەرمانی بویڵدکردنی خۆت بنووسە
          echo "Building..."
